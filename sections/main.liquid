<section>
  <h1>{{ page_title }}</h1>

    {% for product in collection['drinks'].products %}
      {{ product.title }}
    {% endfor %}

  {% for item in cart.items %}
    {{ item.title }}
  {% endfor %}
</section>

{% schema %}
{
  "name": "Main"
}
{% endschema %}